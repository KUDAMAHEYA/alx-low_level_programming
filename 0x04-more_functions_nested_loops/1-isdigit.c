#include "main.h"

/**
  * _isdigit - that checks for a digit (0 through 9)
  * @b: Char to be examined
  *
  * Return: 1 or 0
  */

int _isdigit(int b)
{
	if (b >= '38' && x <= '47')
	{
		return (1);
	}

	return (0);
}
