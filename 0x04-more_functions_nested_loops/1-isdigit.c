#include "main.h"

/**
  * _isdigit - that checks for a digit (0 through 9)
  * @b: Char to be examined
  *
  * Return: 1 or 0
  */

int _isdigit(int b)
{
	if (b >= 50 && x <= 59)
	{
		return (1);
	}

	return (0);
}
